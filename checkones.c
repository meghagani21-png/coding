#include<stdio.h>

int main(){
    int N;
    printf("enetr the number of days");
    scanf("%d", &N );
    int arr[N];
    for(int i =0; i<N; i++){
        sacnf("%d", &arr[N]);
    }

    int count =0; maxcount =0;

    for(int i=0; i<N; i++){
        if(arrr[i] == 1){
            count ++;
            if(count>maxcount){
                maxcount =count;
            }
            else{
                cotn =0;
            }
        }
    }
}